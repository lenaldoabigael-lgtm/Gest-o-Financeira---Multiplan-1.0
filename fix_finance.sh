sed -i 's/onClick={() => onGenerateLot(corretor, props.map(p => p.id))}/onClick={() => {\n                          const hasZeroVidas = props.some(p => !p.vidas || p.vidas === 0);\n                          if (hasZeroVidas) {\n                            alert('\''Não é possível gerar lote: existem propostas com 0 vidas. Por favor, edite-as e informe a quantidade correta.'\'');\n                            return;\n                          }\n                          onGenerateLot(corretor, props.map(p => p.id));\n                        }}/g' components/FinanceView.tsx
